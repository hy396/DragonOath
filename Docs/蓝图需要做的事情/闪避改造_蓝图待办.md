# 闪避（Dodge）技能 蓝图/资产配置待办

> 重建说明：原「冲刺双击（Dash）」方案已改名为「闪避（Dodge）」。
> 旧 `GA_Dash.uasset` 已删除、`IA_Dash` 不再存在；当前 `GA_Dodge.uasset` 继承自 C++ 基类 `DOGameplayAbility_Dash`，
> 输入标签改用 `InputTag.Ability.Dodge`。本文档由当前工程实际状态重建（旧 IDE 历史已丢失，git 中亦无实质内容）。

## 已完成（✅ 无需再做）

1. **C++ 基类 `DOGameplayAbility_Dash` 就绪**：`OnInputTriggered` 激活策略；
   `ActivationOwnedTags` 加 `Status.Dashing`、`ActivationBlockedTags` 加 `Status.Dashing`（冲刺中不可再冲刺）；
   用 `ApplyRootMotionConstantForce` 做朝向位移；结束正常时施加 `DashAttackWindowEffectClass`（冲刺攻击窗口）。
2. **`InputTag.Ability.Dodge` 已声明**（`DOGameplayTag.h/.cpp`，值 `InputTag.Ability.Dodge`）。
3. **`IA_dodge` 输入动作已创建**；`IMC_Default` 中 `IA_dodge` 映射已挂 `InputTriggerRepeatedTap`（双触触发）。
4. **`DA_InputConfig.AbilityInputActions` 已包含 `IA_dodge → InputTag.Ability.Dodge`**。

## 待办（⬜ 这就是当前该做的事）

### 1. 在 `DA_AbilitySet` 中授予 `GA_Dodge`（最高优先级 ⚠️）
- 现象：当前 `DA_AbilitySet.uasset` 里读不到任何技能引用——原 `GA_Dash` 的授予关系随删除一起丢失，
  `GA_Dodge` 在整个工程里也未被任何资产引用。结果：玩家当前**完全无法使用闪避**。
- 操作：在 `DA_AbilitySet` 的 `GrantedAbilities` 新增一条 `FDOAbilityGrant`：
  - `AbilityId = Ability.Dodge`（C++ 已在 `DOGameplayTag` 声明原生标签 `Ability.Dodge`，需先重新编译 / Live Coding 才出现在下拉框）
  - `AbilityClass = GA_Dodge`
  - `InitialLevel = 1`
  - `MaxLevel = 1`
  - `TriggerType = Input`
  - `InputTag = InputTag.Ability.Dodge`（须与 `DA_InputConfig` 中 `IA_dodge` 的标签一致，按键才能路由过来）
  - `EventTag` 留空（`TriggerType = GameplayEvent` 才填）
- 提醒：打开 `DA_AbilitySet` 先确认它现在是否为空。若原本还授予了普攻/跳跃等其他技能且也已丢失，需一并补回。

### 2. 编辑器内确认 `IMC_Default` 中 `IA_dodge` 绑定的物理键
- 旧方案是「双击 A 或 D」触发。打开 `IMC_Default` 确认 `IA_dodge` 的 `RepeatedTap` 触发器绑在预期键上
  （A 与/或 D，或改为独立闪避键如 Shift / Space）。
- 同时确认触发器参数：`NumberOfTapsWhichTriggerRepeat = 2`、`RepeatDelay = 0.25`。
  （注：二进制里能看到 `InputTriggerRepeatedTap` 存在，但绑定的具体按键需在编辑器确认。）

### 3.（可选）C++ 命名一致性清理
- `DOGameplayTag.h` 中的 `InputTag.Dash`（值 `"InputTag.Dash"`）已无人使用，建议迁移/删除，统一用 `InputTag.Ability.Dodge`。
- `DOGameplayAbility_Dash` 类名语义仍偏「冲刺」。若想与「闪避」对齐可改名 `DOGameplayAbility_Dodge`，
  但需同步改 `GA_Dodge` 蓝图父类，**改动较大、谨慎处理**。
- `Status.Dashing` / `Status.DashAttackWindow` 仍被基类与 `DOGameplayAbility_DashAttack` 使用，**保留**。

### 4.（按需）闪避攻击窗口
- 基类结束时会施加 `DashAttackWindowEffectClass`（若 `GA_Dodge` 配了该 GE）。
- 若希望闪避后接普攻触发「闪避攻击」：配置对应 GE 与 `GA_DashAttack` 蓝图，并确保 `Status.DashAttackWindow` 流转正确。
- 若不需要：保持 `GA_Dodge` 的 `DashAttackWindowEffectClass` 为空即可。

## 验证步骤
- PIE Listen Server + 1 Client：双击 A/D（或设定键）→ 角色进入闪避位移并带 `Status.Dashing`；
  期间普攻被 `ActivationBlockedTags`（`Status.Dashing`）拦截，符合预期即通过。
